name: wedding_organise
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  animate_on_hover: ^1.0.2
  cupertino_icons: ^1.0.2
  dio: ^5.3.2
  firebase_auth: ^4.8.0
  firebase_core: ^2.15.1
  firebase_database: ^10.2.5
  flutter:
    sdk: flutter
  flutter_map: ^6.1.0
  flutter_mobx: ^2.0.6+5
  google_fonts: 6.1.0
  image_picker_web: ^3.1.1
  latlong2: ^0.9.0
  lottie: ^2.7.0
  mobx: ^2.2.0
  simple_icons: ^10.1.3
  url_launcher: ^6.1.14
  video_player: ^2.8.3

dev_dependencies:
  build_runner: ^2.4.0

  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  mobx_codegen: ^2.3.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/logo/

    - assets/lottie/initialize.json
    - assets/lottie/services_lottie.json
  #   - images/a_dot_ham.jpeg
